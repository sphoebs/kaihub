


function generateBinaryClassifiers(numClassifiers, betaDistributionParameters, correlation) {

    const classifiers = []

    if (distribution.type = 'normal') {

    }



    return classifiers

}

function classifyBinary(classifier, item) {

    const classEstimation = {
        classifier,
        item,
        classProbabilities:[],
        estimatedClass: null, // class corresponding to max probability
        probability: null,// corresponding probability
    }

    const accuracy = classifier.accuracyifTrueClassIs(item.trueClass)
    const randomNumber = Math.random()
    if (randomNumber<accuracy) estimation =

    return classEstimation
}



